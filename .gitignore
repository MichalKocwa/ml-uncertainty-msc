# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints
*.ipynb

# Data (large files — track with DVC or manually whitelist small samples)
data/raw/
data/processed/
# CC BY-NC data fetched by scripts/fetch_data.py — not vendored, see docs/datasets.md
data/uci_splits/
*.csv
!results/*.csv
!results/**/*.csv
*.parquet
*.h5
*.hdf5
*.pkl
*.pickle

# Model checkpoints
models/checkpoints/
*.pt
*.pth
*.ckpt
*.safetensors

# Experiment tracking
mlruns/
wandb/
lightning_logs/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Misc
*.log
.env

# Claude working notes
.claude_notes.md

# Kept on disk, excluded from this branch — nothing here carries a number that
# chapters 4-5 need, and each has a committed digest or source. Regenerate with
# the commands in README.md.
#
# 13 MB of raw per-step logs; the digest is results/logvar_clamp_diagnostic_summary.csv
# and results/logvar_clamp_contact_check.csv, both committed (D29).
results/logvar_clamp_diagnostic.csv
# 144 exploration PNGs (24 MB). The CSVs they were plotted from —
# results/depth_exploration/ — stay committed, so no measurement is lost.
figures/depth_exploration/
